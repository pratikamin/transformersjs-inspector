/**
 * What a host page would load as `<script type="module" src="…/dist/preload.js">`: the
 * side-effect entry, nothing else. It must be the *first* module script on the page.
 */
import '../src/preload.ts';
